# DisasterLens AI - Dependencies
# Streamlit Cloud handles these automatically

# ── Core ──
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# ── ML — Classic ──
scikit-learn>=1.3.0
joblib>=1.3.0

# ── ML — Deep Learning (CPU) ──
transformers>=4.36.0
torch>=2.1.0
datasets>=2.16.0

# ── NLP ──
spacy>=3.7.0
nltk>=3.8.0

# ── Geo & Maps ──
geopy>=2.4.0
folium>=0.15.0
streamlit-folium>=0.15.0

# ── Visualization ──
plotly>=5.18.0
wordcloud>=1.9.0

# ── Social Media Feeds ──
praw>=7.7.0
atproto>=0.0.46

# ── Twitter/X (dormant hook — only needed when enterprise client activates) ──
tweepy>=4.14.0

# ── spaCy model ──
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0.tar.gz
